<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (always dark, no white flash before Compose renders).
         Framework theme so we don't depend on Material Components (app is 100% Compose). -->
    <style name="Base.Theme.Nexusmarvelapp" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/nexus_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.Nexusmarvelapp" parent="Base.Theme.Nexusmarvelapp" />

    <!-- Splash screen theme shown at cold start. -->
    <style name="Theme.Nexusmarvelapp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/nexus_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Nexusmarvelapp</item>
    </style>
</resources>
